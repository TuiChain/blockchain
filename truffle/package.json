{
    "description": "Blockchain component for the TuiChain application",
    "repository": {
        "type": "git",
        "url": "https://github.com/TuiChain/blockchain.git",
        "directory": "truffle"
    },
    "license": "UNLICENSED",
    "scripts": {
        "prettier": "prettier --write 'contracts/**/*.sol' 'test/**/*.js'",
        "prettier-check": "prettier --check 'contracts/**/*.sol' 'test/**/*.js'",
        "solhint": "solhint -w 0 'contracts/**/*.sol'",
        "test": "npm run prettier-check && npm run solhint && truffle test",
        "truffle": "truffle"
    },
    "dependencies": {
        "@openzeppelin/contracts": "^3.3.0",
        "truffle": "^5.1.59"
    },
    "devDependencies": {
        "@openzeppelin/test-helpers": "^0.5.10",
        "bn.js": "^4.11.0",
        "prettier": "1.19.1",
        "prettier-plugin-solidity": "1.0.0-beta.1",
        "solhint": "^3.3.2"
    }
}
